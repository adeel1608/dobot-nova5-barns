# This ConfigMap is generated from the config/tasks.json file
# DO NOT edit this file manually
# 
# To create/update this ConfigMap, run:
# kubectl create configmap routine-tasks-config --from-file=tasks.json=config/tasks.json -n barns --dry-run=client -o yaml | kubectl apply -f -
#
# Or to generate this file:
# kubectl create configmap routine-tasks-config --from-file=tasks.json=config/tasks.json -n barns --dry-run=client -o yaml > k8s/configmaps/routine-tasks-config.yaml






