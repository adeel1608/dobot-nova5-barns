# This ConfigMap is generated from the data directory
# DO NOT edit this file manually
# 
# To create/update this ConfigMap, run:
# kubectl create configmap scheduler-data --from-file=data/ -n barns --dry-run=client -o yaml | kubectl apply -f -
#
# Or to generate this file:
# kubectl create configmap scheduler-data --from-file=data/ -n barns --dry-run=client -o yaml > k8s/configmaps/scheduler-data-configmap.yaml
