apiVersion: v1
kind: Service
metadata:
  name: scheduler-service
  namespace: barns
  labels:
    app: scheduler-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: scheduler-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scheduler-service
  namespace: barns
  labels:
    app: scheduler-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scheduler-service
  template:
    metadata:
      labels:
        app: scheduler-service
    spec:
      imagePullSecrets:
      - name: gcr-json-key
      containers:
      - name: scheduler-service
        image: me-central2-docker.pkg.dev/qss-development-project/barns/barns-scheduler:latest
        imagePullPolicy: Always
        env:
        - name: RABBITMQ_URL
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: url
        - name: PYTHONPATH
          value: "/app"
        - name: DEBUG
          value: "false"
        - name: TELEGRAF_HOST
          value: "telegraf"
        - name: TELEGRAF_PORT
          value: "8094"
        volumeMounts:
        - name: data
          mountPath: /app/data
        livenessProbe:
          exec:
            command:
            - python
            - -c
            - "import socket; socket.create_connection(('rabbitmq', 5672), timeout=5)"
          initialDelaySeconds: 50
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
      volumes:
      - name: data
        emptyDir: {}






